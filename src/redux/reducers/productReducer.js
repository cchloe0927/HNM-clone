//Action Values

//Action Creator

//Initial
let initialState = {};

//reducer
function productReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

//export
export default productReducer;
